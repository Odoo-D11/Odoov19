<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="ir_cron_send_pending_legalization_mail" model="ir.cron">
        <field name="name">Enviar correo de legalización pendiente (Viáticos)</field>
        <field name="model_id" ref="model_expense_expense"/>
        <field name="state">code</field>
        <field name="code">model.send_pending_legalization_mail()</field>
        <field name="interval_number">5</field>
        <field name="interval_type">hours</field>
        <field name="priority">3</field>
    </record>

</odoo>